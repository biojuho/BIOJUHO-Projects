anthropic>=0.45.0,<1.0
requests>=2.32.0,<3.0
beautifulsoup4>=4.12.3,<5.0
lxml>=5.0.0,<6.0
schedule>=1.2.0,<2.0
python-dotenv>=1.0.0,<2.0
notion-client>=2.2.1,<3.0
gspread>=6.0.0,<7.0
google-auth>=2.27.0,<3.0
colorama>=0.4.6,<1.0
aiohttp>=3.10.0,<4.0
# [v14.0] Gemini Embedding 2 (의미적 클러스터링)
google-genai>=1.0.0
# Dashboard (선택)
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
# PostgreSQL (선택: DATABASE_URL=postgresql://... 사용 시)
# psycopg2-binary>=2.9.9,<3.0
asyncpg>=0.29.0,<1.0           # PostgreSQL 비동기 드라이버 (asyncpg Pool 사용 시)
# HTML 파싱 가속 (선택: beautifulsoup4 대비 ~10배 빠름)
selectolax>=0.3.21,<1.0        # Lexbor 백엔드 CSS 셀렉터 파싱
# X/Twitter 비공식 클라이언트 (선택: Jina 폴백 대안)
twikit>=2.3.0,<3.0
# [Phase 1] Instructor — Pydantic 기반 구조화된 LLM 출력 (JSON 파싱 안정화)
instructor>=1.14.5
# [Phase 2] Kiwipiepy — 한국어 형태소 분석 (AI어투 탐지 + 신조어 감지)
# 주의: Windows 한글 경로에서 segfault 이슈. 문자열 패턴 폴백으로도 동작.
# pip install kiwipiepy  # (선택 — Linux/WSL 권장)
# [Phase 3] DeepEval — LLM 출력 품질 자동 평가 (환각/관련성/일관성)
deepeval>=3.9.0
# [Phase 4] Scrapling — 안티봇 우회 + 적응형 뉴스 스크래핑
scrapling>=0.4.0
prometheus_client>=0.21.0
structlog>=24.0.0
