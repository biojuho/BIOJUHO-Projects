# Core dependencies are defined in pyproject.toml.
# Install with: pip install -e .
#
# Optional extras:
#   pip install -e .[dashboard]
#   pip install -e .[viz]
#   pip install -e .[market]
#   pip install -e .[social]
#   pip install -e .[dev]
#
# This file is kept for backwards compatibility with tools that expect it.
# To regenerate a locked requirements file, use:
#   pip-compile pyproject.toml -o requirements.lock

python-dotenv>=1.0.0
notion-client>=2.2.1,<3.0
mcp>=1.0.0,<2.0
feedparser>=6.0.0,<7.0
httpx>=0.27.0,<1.0
python-dateutil>=2.9.0,<3.0
google-genai>=1.0.0,<2.0
anthropic>=0.40.0,<1.0
openai>=1.0.0,<2.0
tenacity>=8.0.0,<10.0
