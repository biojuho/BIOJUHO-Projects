# 핸드오프 NNNN — <한 줄 제목>

- **상태:** OPEN  <!-- OPEN → CLAIMED → DONE -->
- **기획자:** Claude Code
- **추천 실행자:** Codex | Antigravity | (비워두면 먼저 집는 쪽)
- **실행자:** —  <!-- CLAIMED 때 본인 이름 -->
- **작성일:** YYYY-MM-DD

## 목표
이 일이 끝나면 무엇이 참이 되는가. 한두 문장.

## 배경
왜 하는가. 관련 파일 경로를 가리킨다.

## 범위
- **건드릴 것:** 바꿔도 되는 파일·영역.
- **건드리지 말 것:** 손대면 안 되는 코드·데이터·설정.
  - **기본 금지(모든 핸드오프에 항상 적용):** `STATE.md`·`WORKLOG.md`는 기획자 전용이다.
    실행자는 **읽기만 하고 쓰지 않는다.** 진행 상황·결과 보고는 이 파일의 반환 섹션에만 적고,
    정본 기록으로 옮기는 일은 기획자가 검수한 뒤에 한다.

## 단계
1. …
2. …
3. …

## 수용 게이트
- 통과해야 완료인 검증(테스트·린트·빌드 명령 등).
- **결론을 여기 적지 않는다.** 기대하는 답("A가 맞다", "3건일 것")을 게이트에 미리 쓰면 실행자가 그걸 베껴 오고, 그러면 검증이 불가능해진다. 무엇을 확인해야 하는지만 적고 답은 실행자가 계산하게 둔다.

## 금지사항
- 되돌릴 수 없는 외부 행위는 사용자 승인 없이 금지. 비밀키 노출 금지.
- 범위 밖으로 넘지 말 것. 막히면 아래 반환 섹션에 적어 되돌릴 것.

## 재작업 상한

- 반환 후 기획자 검수에서 지적이 나오면 **재작업은 1회까지**. 2회차에도 남으면 되돌려 보내지 말고 `남은 것 / 막힌 곳`에 적어 사용자 판단으로 올린다.
- 검수자는 지적할 때 **무엇을 어떻게 고치면 되는지**까지 적는다. "다시 보라"만 적고 넘기지 않는다.

---

## 반환 섹션 (실행자가 채운다)
- **결과:** 무엇을 했는가. 바뀐 파일.
- **실행한 게이트:** 돌린 명령과 통과 여부.
- **남은 것 / 막힌 곳:** 기획자가 이어받을 부분.
