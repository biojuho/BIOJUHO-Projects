listener 1883
# Local development keeps anonymous MQTT enabled so docker-compose can start without
# pre-provisioned device credentials. Production should use password_file plus an
# ACL such as mosquitto.acl.example, or Mosquitto dynamic security.
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
log_dest stdout
