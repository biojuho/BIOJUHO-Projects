# Maintenance Tasks

Last updated: 2026-04-14

## Completed
- Added `prop-types` and PropTypes for VC components.
- Cleaned unused imports in `VCDashboard`.
- Ran `npm install`, `npm run lint`, and `npm test`.
- Added `.nvmrc` to align Node version with engines.

## Pending
- Review and resolve `npm audit` vulnerabilities (12 total at last check).
  - Recommend running `npm audit` to inspect, then decide on `npm audit fix` or targeted updates.
