import React from 'react';
import ReactDOM from 'react-dom/client';
import CanvaSearchDesigns from './canva-search-designs';

const root = document.getElementById('root');
if (root) {
  ReactDOM.createRoot(root).render(
    <React.StrictMode>
      <CanvaSearchDesigns />
    </React.StrictMode>
  );
}










