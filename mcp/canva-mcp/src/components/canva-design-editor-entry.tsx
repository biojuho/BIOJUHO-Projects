import React from 'react';
import ReactDOM from 'react-dom/client';
import CanvaDesignEditor from './canva-design-editor';

const root = document.getElementById('root');
if (root) {
  ReactDOM.createRoot(root).render(
    <React.StrictMode>
      <CanvaDesignEditor />
    </React.StrictMode>
  );
}










