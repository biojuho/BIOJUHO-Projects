# STATE — 지금 상태

이 파일은 **모든 에이전트(Claude·Codex·Antigravity·Grok 등)가 세션을 시작할 때 가장 먼저 읽는 한 장**이다.
"이미 끝난 일"은 `WORKLOG.md`에, **"지금 하는 중"과 "다음에 할 것"은 여기에** 적는다.

## 이 프로젝트

- **별칭:** BIOJUHO-Projects, getdaytrends, 겟데이트렌드, 실시간 트렌드 조사, X 트렌드 레이더, 커뮤니티 원문 레이더, 바이럴 조기 탐지
- **한 줄 정의:** X(트위터)·커뮤니티·뉴스 원문을 멀티소스로 수집해 바이럴 조기 신호를 점수화하고 로컬 대시보드로 보여주는 트렌드 레이더. 정본 경로는 `automation/getdaytrends/`.
- **실행 방법:** `cd automation/getdaytrends && ../../.venv/bin/uvicorn dashboard:app --host 127.0.0.1 --port 8010` → http://127.0.0.1:8010
- **테스트:** `.venv/bin/python -m pytest automation/getdaytrends/tests -q` (2026-08-06 기준 801 passed · 7 skipped — Kiwipiepy·Scrapling 미설치)
- **갱신:** 2026-08-06 · by Claude

> **수집은 브라우저가 열려 있을 때만 돈다.** 자동 갱신이 서버 스케줄러가 아니라 대시보드 페이지의
> `setInterval`(X 레이더·급속 바이럴 2분, 라이브 레퍼런스 10분)로 구현돼 있다. 탭을 닫으면 데이터가 그 시점에 멈춘다.

## 지금 진행 중

| 스레드 | 담당 | 상태 | 다음 액션 |
|--------|------|------|-----------|
| 2026-08-06 아침 라운드(필터·관측·수집 3건) | (Claude 아닌 도구) | 코드 완료·**미커밋 30건** | 커밋 여부 판단 → 커밋하거나 되돌리기 |

## 사용자 결정 대기

- **미커밋 30건 처리.** 오늘 아침 작업(신규 파일 14개 포함)이 커밋되지 않은 채 남아 있다. 테스트 801개는 통과 상태다. 커밋할지, 더 다듬을지 결정 필요.
- **수집 상시화 여부.** 지금은 브라우저를 열어둬야 수집이 돈다. "실시간"을 유지하려면 서버측 백그라운드 스케줄러가 필요한데, 그러면 자리를 비운 사이에도 외부 요청이 계속 나간다 — 수집 빈도·대상 경계를 먼저 정해야 한다.

## 최근 마감 3건

- 2026-08-06 · 공개 X 90초 캐시·수동 우회·2분 자동 갱신, 동일 표본 중복 관측 방지, 개드립·더쿠 HOT·루리웹 베스트 직접 수집, X 네이티브 급등 레인 (테스트 801)
- 2026-08-06 · Orca 협업 검토 반영 — 시간 감쇠·공개 X 순위·교차출처 클러스터·관측 증가량·점수 버전·근거 신뢰도 적용 (테스트 790)
- 2026-08-06 · 커뮤니티·X 원문 레이더에 부동산·정치 제외 추가, 스포츠 누락 표현 보강, 라이브 금지어 0건 (테스트 783)

## 이 프로젝트 경계

- 스포츠·정치·증시·종목·기업 실적·부동산은 표시하지 않는다. 자동 문안 생성·게시 기능도 없다.
- 수집은 공개 데이터 대상이며, 되돌릴 수 없는 외부 행위(게시·배포·결제·외부 전송)는 사용자 명시 승인 없이 금지.
- 비밀키·토큰·자격증명 출력·외부 전송 금지.

---

**끝낼 때:** 이 파일의 `지금 진행 중`·`갱신` 줄을 손보고, 완료분은 `WORKLOG.md`에 한 줄 추가한다.
