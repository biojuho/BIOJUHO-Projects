# AGENTS.md — 실행자(Executor) 진입점

이 파일은 **실행자 LLM(Codex, Antigravity 등)**이 이 프로젝트에서 세션을 시작할 때 읽는 짧은 진입점이다.
**기획은 Claude Code가 한다.** 너(실행자)는 Claude가 적어 둔 플랜을 받아 **실제로 수행**한다.

> 이 협업은 **기본값(우선순위)일 뿐 강제가 아니다.** 작은 일은 기획자가 직접 해도 된다.

## 세션을 시작할 때 — 반드시 이 순서로 읽는다

1. **`STATE.md`** — 지금 무엇을 하는 중이고 다음 액션이 무엇인지. **가장 먼저 읽는다.**
2. **`WORKLOG.md` 최근 5줄** — 이미 끝난 일을 다시 하지 않기 위해.
3. **`handoffs/`의 OPEN·CLAIMED 파일** — 나에게 넘어온 플랜이 있는지.
4. `git status` — 다른 에이전트가 커밋하지 않고 떠난 변경이 있는지.

사용자가 **폴더 이름 대신 하던 일로** 말하면("그 트렌드 조사하던 거"), 현재 폴더에서 찾지 말고
**`~/.claude/PROJECTS.md`의 별칭 인덱스**에서 경로를 먼저 찾는다.

## 끝낼 때 — 무조건 기록한다. 단, **누가 쓰는지가 다르다**

### 실행자라면 (핸드오프를 받아 수행한 경우)

**`WORKLOG.md`·`STATE.md`를 쓰지 않는다.** 읽기만 한다. 네가 남기는 기록은 **핸드오프의 반환
섹션 하나뿐**이다 — 결과·실행한 게이트·남은 것을 적고 상태를 `DONE`으로 바꾸면 끝이다.
정본 기록으로 옮기는 일은 기획자가 검수한 뒤에 한다.

이유는 둘이다. 실행자 여럿이 동시에 돌면 같은 두 파일에서 서로를 덮어쓴다. 그리고 검수 전에
실행자의 자기 보고가 정본으로 굳으면, 나중에 틀린 것이 드러나도 되돌릴 자리가 없다.

### 기획자라면 (헤더 · 사용자에게 직접 일을 받은 경우)

1. **`WORKLOG.md`에 한 줄 추가** (append-only, 최신이 맨 아래):
   `- YYYY-MM-DD · 무엇 · 파일: 핵심경로 · by <기획자> 기획 | <실행자> 실행`
2. **`STATE.md` 갱신** — 끝난 스레드를 지우고 남은 다음 액션과 `갱신` 줄을 손본다.
   중간에 멈출 때가 특히 중요하다. 남기지 않으면 다음 에이전트는 그 일이 있었다는 것조차 모른다.

## 네 역할

1. `handoffs/`에서 상태가 **OPEN**인 핸드오프 플랜을 하나 집는다.
2. 집을 때 상태를 **CLAIMED**로 바꾸고 본인 이름을 적는다 (같은 일을 둘이 동시에 잡지 않게).
3. 플랜의 단계대로 수행한다. **범위(건드릴 것/건드리지 말 것)를 임의로 넘지 않는다.**
4. 플랜의 수용 게이트(테스트·린트·빌드 등)를 돌려 통과를 확인한다.
5. 끝나면 핸드오프의 **반환 섹션**에 결과·실행한 명령·남은 것을 적고 상태를 **DONE**으로 바꾼다.
6. 막히면 추측으로 진행하지 말고, 막힌 지점을 반환 섹션에 적어 기획자(Claude)에게 되돌린다.

## 절대 금지 (경계)

- 되돌릴 수 없는 외부 행위(배포·게시·삭제·결제·외부 전송)는 사용자 명시 승인 없이 하지 않는다.
- 비밀키·토큰·자격증명을 출력하거나 외부로 보내지 않는다.
- 범위 밖 파일·운영 데이터·설정을 임의로 바꾸지 않는다.

## 더 읽을 것

- `CLAUDE.md` — 이 저장소의 기존 개발·검증 계약
- `handoffs/README.md` — 핸드오프 폴더 운영 규칙
- `handoffs/TEMPLATE.md` — 플랜 양식
- `automation/getdaytrends/README.md` — GetDayTrends 실행·구조 안내
- GetDayTrends 기본 게이트: `uv run pytest automation/getdaytrends/tests -q`

